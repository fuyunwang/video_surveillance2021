spring:
  application:
    name: config-client
  cloud:
    config:
      uri: http://localhost:8888/
      fail-fast: true
    nacos:
      discovery:
        server-addr: ${nacos.url}
  profiles:
    active: dev
server:
  port: 8762


